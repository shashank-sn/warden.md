# cli

the conformance toolkit. `check` verifies that a service is agent ready: its auth.md file, its metadata endpoints, and the discovery handshake an agent performs.

status: not built yet. work is tracked in the [`check` issues](https://github.com/shashank-sn/warden.md/issues?q=label%3Atrack%3Acheck).

- read-only by default, active probe opt-in
- human output and `--json` for ci
- exit codes that mean something in a pipeline
