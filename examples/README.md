# examples

runnable demos that prove the pieces work together. nothing here yet.

planned:

- a demo resource server that publishes its own auth.md and metadata
- an agent script that registers, gets approved, makes one scoped call, and completes
- a local end-to-end run of agent, broker, and service in one command
